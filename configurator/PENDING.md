# Pending Items

## Auth / Session

1. **Forgot password flow** — Requires building an email-sending framework first (SMTP service, token generation, expiry, reset endpoint).

## UI / UX

2. **At-a-glance status indicators** — Better visuals on list views (Jobs, Clients, Groups) to show execution status, connection state, and enabled/disabled state without drilling into detail pages.

3. **Client execution status within job view** — Job details should show per-client execution status (which clients have completed, failed, pending, etc.) rather than just an aggregate status.
