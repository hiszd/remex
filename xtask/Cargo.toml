[package]
name = "xtask"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
glob = "0.3"
pulldown-cmark = "0.12"
regex = "1.10"
serde = { version = "1", features = ["derive"] }
walkdir = "2.5"
toml = "0.8"

[workspace]
